import React from 'react';

// This component is part of the new design and is not used in the reverted version.
// Clearing its content to prevent it from being rendered.
const OffersSection: React.FC = () => {
  return null;
};

export default OffersSection;